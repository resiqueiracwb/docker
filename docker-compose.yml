version: '3'

services:
  springboot-docker-compose-app-container:
    image: repo:1
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - /data/spring-boot-docker-compose
    ports:
      - "8081:8081"